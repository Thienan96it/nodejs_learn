<div class="mt-4">
  <div class="row">
    <div class="col-lg-3">
      <button>Learn now !!</button>
    </div>

    <div class="col-lg-9">
      <h2>{{course.name}}</h2>
      <iframe
        width="600"
        height="468"
        src="https://www.youtube.com/embed/{{course.videoId}}"
        allowfullscreen
      ></iframe>
      <p>{{course.description}}</p>
    </div>
  </div>
</div>